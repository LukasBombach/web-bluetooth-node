{
  "name": "btleplug-neon-bindings",
  "version": "0.1.0",
  "license": "ISC",
  "main": "index.node",
  "scripts": {
    "dev": "yarn build && echo '\nstarting\n' && yarn start",
    "start": "node index.js",
    "build": "neon build --release"
  },
  "devDependencies": {
    "neon-cli": "0.8.1"
  }
}
