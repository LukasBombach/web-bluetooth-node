/* const { person_new, person_set_name, person_greet } = require(".");

class Person {
  constructor(firstName, lastName) {
    this.boxed = person_new(firstName, lastName);
  }

  setName(name) {
    // person_set_name(this.boxed, name);
  }

  greet() {
    // person_greet(this.boxed);
    this.boxed.greet();
  }
}

console.log("Creating a person");
const person = new Person("Eddie", "Van Halen");

console.log("Greeting");
console.log(person.greet());
 */

const addon = require(".");

console.log("xyz");
